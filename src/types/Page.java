package types;

/**
 * Structure representing the contents of a Page.  No point having private
 * fields with getters/setters, so just make everything public.
 */
public class Page {
    public String address;
    public String[] links;
}
