package types;

/**
 * Structure representing the pages in the Internet.  No point having private
 * fields with getters/setters, so just make everything public.
 */
public class Internet {
    public Page[] pages;
}
